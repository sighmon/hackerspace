<% flash.each do |type, message| %>
	<div class="alert alert-dismissable <%= bootstrap_class_for(type) %> fade in">
		<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
		<%= message %>
	</div>
<% end %>